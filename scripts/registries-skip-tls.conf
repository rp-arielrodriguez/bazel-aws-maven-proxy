# Registries configuration used when SKIP_TLS_VERIFY=true.
# insecure=true allows TLS connections with untrusted/replaced certificates,
# which is needed when a corporate proxy intercepts HTTPS traffic.

unqualified-search-registries = ["docker.io"]

[[registry]]
location = "docker.io"
insecure = true
